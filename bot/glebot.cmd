@echo off
rem Запуск Глебота: из корня репозитория (там рубрика) и с дописыванием в лог.
rem Отдельный файл, а не строка в планировщике: там кавычки и перенаправление
rem вывода живут плохо, а логи нужны — иначе упавший бот молчит неотличимо
rem от бота, которому просто нечего сказать.
cd /d "%~dp0.."
python -u bot\app.py >> glebot.log 2>&1
